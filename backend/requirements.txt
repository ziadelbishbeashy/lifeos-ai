Flask>=3,<4
Flask-SQLAlchemy>=3,<4
Flask-Login>=0.6,<1
Flask-Migrate>=4,<5
Flask-WTF>=1.2,<2
SQLAlchemy>=2,<3
psycopg[binary]>=3.2,<4
python-dotenv>=1,<2
google-genai
openai
pypdf>=6,<7
gunicorn
PyMuPDF>=1.24,<2
Pillow>=10,<13
pytesseract>=0.3.13,<1
opencv-python-headless>=4.10,<5

# I17 schedule timezone database (required on Windows for IANA zones)
tzdata>=2025.2,<2027

# Optional tracing/observability (disabled unless LANGSMITH_TRACING=true)
langsmith>=0.4.43,<1
# Optional I19 orchestration. LifeOS keeps deterministic planning, tools, I9 and DB ownership.
# LangGraph brings langchain-core transitively; no generic autonomous agent executor is used.
langgraph>=1,<2

# Authentication V1: Google OpenID Connect ID-token verification + token exchange transport.
google-auth>=2,<3
requests>=2,<3
