# Foundation V2 baseline provenance

Foundation V2 was created from the user-provided archive:

`lifeos-ai-feature-hybrid-rag (1).zip`

SHA-256:

`ee955a07dc3f82043c3a0d121b34372eef3c280ab930e321bee9065d8cd0beab`

The original backend source was copied in full before the architecture boundary
changes. The old project contained 108 `test_*.py` files and 56 service modules;
Foundation V2 preserves all of them and adds architecture/API tests rather than
replacing the old suite.
