import { useState,type FormEvent } from "react";
import { login } from "../auth/session";
import { navigate } from "../core/navigation";
import { ApiError } from "../api/client";
import { PublicShell } from "../public/PublicShell";
function safeNext(){const v=new URLSearchParams(window.location.search).get("next")||"/dashboard";return v.startsWith("/")&&!v.startsWith("//")?v:"/dashboard"}
export function LoginPage(){const[error,setError]=useState<string|null>(null),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);setBusy(true);setError(null);try{await login({email:String(d.get("email")||""),password:String(d.get("password")||""),remember:d.get("remember")!==null});navigate(safeNext(),true)}catch(x){setError(x instanceof ApiError?x.message:"Login failed.")}finally{setBusy(false)}}return <PublicShell><main className="authentication-page"><section className="authentication-introduction"><span className="hero-label">Welcome back</span><h1>Continue building your intelligent workspace.</h1><p>Access your projects, tasks, notes, documents and execution plans.</p><div className="authentication-benefits"><span>Connected project knowledge</span><span>Private personal workspace</span><span>Clear execution priorities</span></div></section><section className="authentication-card"><div className="authentication-heading"><span>LifeOS account</span><h2>Log in</h2><p>Enter your account details to continue.</p></div>{error?<div className="public-flash error">{error}</div>:null}<form className="authentication-form" onSubmit={submit}><div className="authentication-field"><label htmlFor="email">Email address</label><input type="email" id="email" name="email" placeholder="name@example.com" autoComplete="email" required/></div><div className="authentication-field"><label htmlFor="password">Password</label><input type="password" id="password" name="password" placeholder="Enter your password" autoComplete="current-password" required/></div><label className="remember-control"><input type="checkbox" name="remember"/><span>Keep me logged in</span></label><button type="submit" className="authentication-submit" disabled={busy}>{busy?"Logging in…":"Log In to LifeOS"}</button></form><p className="authentication-switch">Do not have an account? <a href="/register">Create one</a></p></section></main></PublicShell>}
